export default function SoundsPage() {
    return (
        <div className="animate-fade-in">
            <h1>Sounds</h1>
            <p style={{ color: 'var(--color-text-muted)', marginTop: 'var(--space-sm)' }}>
                MP3 Upload, Trim und Zuordnung – wird in Schritt 5 implementiert.
            </p>
        </div>
    );
}
