export default function GamePage() {
    return (
        <div className="animate-fade-in">
            <h1>Spieltag</h1>
            <p style={{ color: 'var(--color-text-muted)', marginTop: 'var(--space-sm)' }}>
                Operator-Code generieren und verwalten – wird in Schritt 3 implementiert.
            </p>
        </div>
    );
}
